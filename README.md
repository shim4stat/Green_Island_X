# Green_Island_X

TODO: 説明を追加

## Installation
`git clone --recursive https://github.com/shim4stat/Green_Island_X.git`を使用してリポジトリをクローンしてください。
既にclone済み、かつ`contract/lib/openzeppelin-contracts/`が空の場合、`contract`ディレクトリに移動して`forge install`を実行してください。

(TODO: いろいろ確定したらMakefileを追加して簡単にする)